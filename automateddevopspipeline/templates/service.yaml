apiVersion: v1
kind: Service
metadata:
  name: {{ include "automateddevopspipeline.fullname" . }}
  labels:
    {{- include "automateddevopspipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "automateddevopspipeline.selectorLabels" . | nindent 4 }}
